# KASB for x86_64-pc-windows-msvc

This generated artifact contains the x86_64-pc-windows-msvc Node addon and Rust `kasb` CLI from one build. npm users install the root KASB package instead of depending on the platform package directly; direct CLI consumers use the accompanying target archive.
