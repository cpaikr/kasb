# KASB for x86_64-unknown-linux-gnu

This generated artifact contains the x86_64-unknown-linux-gnu Node addon and Rust `kasb` CLI from one build. It requires glibc 2.28 or newer. npm users install the root KASB package instead of depending on the platform package directly; direct CLI consumers use the accompanying target archive.
