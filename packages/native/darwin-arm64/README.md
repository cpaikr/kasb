# KASB for aarch64-apple-darwin

This generated artifact contains the aarch64-apple-darwin Node addon and Rust `kasb` CLI from one build. npm users install the root KASB package instead of depending on the platform package directly; direct CLI consumers use the accompanying target archive.
